alias celar=clear
alias caler=clear
alias claer=clear
alias cler=clear
alias cls=clear


vibe-check () {
    sudo apt update -y
    sudo apt upgrade -y
    sudo apt autoremove -y
    sudo apt clean
}
